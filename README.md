# The Agents Force — theagentsforce.com

Static site for [theagentsforce.com](https://theagentsforce.com), hosted on GitHub Pages.

No frameworks, no CDNs, no build toolchain. Every asset the visitor loads is served from
this repo; the only third-party request on the site is Google Analytics.

---

## Deploying

Push the contents of this folder to the root of `main`. In the repo's
**Settings → Pages**, set the source to **Deploy from a branch → main → / (root)**.

`CNAME` and `.nojekyll` are already here and both need to stay.

---

## Editing the site

Everything you'd normally want to change lives at the top of `build.py`, in the
section marked `CONTENT`:

| What you want to change | Where |
|---|---|
| Hero headline and subhead | `HERO` |
| The three agent cards | `AGENTS` |
| Sprint steps, price | `PROCESS` |
| About copy and the three facts | `ABOUT` |
| Journal posts on `journal.html` | `POSTS` |
| Calendar / LinkedIn / X / YouTube links | `LINKS` |
| Google Analytics property | `GA_MEASUREMENT_ID` |

Then run:

```
python3 build.py
```

That regenerates `index.html`, `journal.html`, `journal-agent-001.html`, `404.html`,
`sitemap.xml` and `robots.txt`. Commit the result.

Design tokens (colours, type scale, spacing, shadows) live at the top of `styles.css`.
That file is hand-maintained — `build.py` never touches it.

### Adding a journal post

1. Add an entry to `POSTS` in `build.py`.
2. Add a `page_*()` function for the article, following `page_article()` as the model.
3. Register it in `main()` and in the `urls` list that builds the sitemap.
4. Run `python3 build.py`.

---

## What's in here

```
index.html  journal.html  journal-agent-001.html  404.html   generated by build.py
sitemap.xml  robots.txt                                       generated by build.py
build.py                     content + page templates (edit this)
styles.css                   design tokens + component CSS (edit this)
fonts/                       Poppins, latin subset, self-hosted (~7 KB each)
assets/
  portrait-hari.jpg              hero + about portrait
  og-cover.jpg                   1200×630 social preview card
  favicon.svg  apple-touch-icon.png
CNAME  .nojekyll               GitHub Pages configuration
```

---

## Analytics

Google Analytics 4 (`G-ZM2HP8YTXB`) runs on every page, including `404.html` — useful for
spotting dead inbound links. The tag is emitted by the `analytics()` function in `build.py`
and sits last in `<head>`: it is `async`, so the browser still discovers it early, but the
stylesheet and font preload get the first round trip.

To remove tracking from the whole site, set `GA_MEASUREMENT_ID = ""` in `build.py` and
re-run it. To swap properties, change the ID and re-run.

Note: the site sets analytics cookies with no consent prompt. If a meaningful share of
traffic comes from the EU or UK, that needs either a consent banner or a cookieless
analytics tool.

## Performance

A first visit to the homepage is **~135 KB across 9 requests**, of which 8 are served from
this repo and one is the Google Analytics tag. The HTML contains the full page text, so
social crawlers and search engines see the content without running any JavaScript.

## Accessibility

- Every colour pair in the palette clears WCAG AA (lowest is 5.5:1).
- Visible keyboard focus on all interactive elements.
- Skip-to-content link, landmark elements, one `<h1>` per page.
- Honours `prefers-reduced-motion`.
